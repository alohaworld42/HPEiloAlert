# ─────────────────────────────────────────────
# Copy this file to .env and fill in your values
#   cp .env.example .env
# ─────────────────────────────────────────────

# iLO connection
ILO_HOST=192.168.1.100
ILO_USERNAME=Administrator
ILO_PASSWORD=your-ilo-password
ILO_VERIFY_SSL=false

# Alert settings
ALERT_THRESHOLD=70           # Fan speed % that triggers an alert
ALERT_SERVER_NAME=DL385-Gen11-01
ALERT_INTERVAL_SEC=120       # Check every N seconds (120 = every 2 min)

# SMTP / email
# ── Option A: Mailpit (local Docker catch-all, no real delivery) ──────────────
# View emails at http://localhost:8025 after running: docker compose up -d
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_TLS=false
SMTP_USERNAME=
SMTP_PASSWORD=
EMAIL_FROM=alerts@localhost
EMAIL_TO=you@localhost

# ── Option B: External SMTP (real delivery) ───────────────────────────────────
# Uncomment and fill in these lines, then comment out Option A above
# SMTP_HOST=smtp.yourcompany.com
# SMTP_PORT=587
# SMTP_TLS=true
# SMTP_USERNAME=alerts@yourcompany.com
# SMTP_PASSWORD=your-smtp-password
# EMAIL_FROM=alerts@yourcompany.com
# EMAIL_TO=infra-team@yourcompany.com  # Comma-separated for multiple recipients
